一：前端
1.准备结构
2.先进行注册：通过form+submit+method+name将表单的数据给后端。
3.注册时先进行用户名唯一性匹配。ajax将用户名传给后端。接收后端匹配的结果。
4.登录：将登录的用户名和密码通过ajax提交给后端.  接收后端返回的结果，判断是否登录成功。
二：后端
1.进行数据库的链接
2.接收前端注册表传入的数据，插入数据库。
3.用户名唯一值的检测。后端获取前端传来的用户名和数据库匹配。查看当前的数据库中是否存在相同的用户名。将查询的结果给前端。
4.登录匹配的检测：接收前端传入的用户名和密码，和数据库的用户名和密码进行匹配，返回结果给前端。

